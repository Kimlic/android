<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools"
  android:id="@+id/phoneItem"
  android:layout_width="match_parent"
  android:layout_height="@dimen/item_user_stage_height"
  android:focusableInTouchMode="false">

  <ImageView
    android:id="@+id/iconIv"
    style="@style/DetailsItemTextView"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:contentDescription="@string/icon"
    tools:background="@drawable/ic_phone_in_talk_dark_24dp"/>

  <ImageView
    android:id="@+id/warningIv"
    style="@style/DetailsItemTextView"
    android:layout_width="16dp"
    android:layout_height="16dp"
    android:layout_marginStart="20dp"
    android:layout_marginBottom="8dp"
    android:layout_alignParentBottom="true"
    android:background="@drawable/ic_profile_warning_red_white_icon"
    android:contentDescription="@string/warning"
    android:gravity="center"
    tools:text="Item description"/>

  <TextView
    android:id="@+id/contentTv"
    style="@style/DetailsItemTextView"
    android:layout_marginStart="48dp"
    android:gravity="center"
    tools:text="Item description"/>

  <ImageView
    android:id="@+id/arrowIv"
    style="@style/DetailsItemShevron"
    android:contentDescription="@string/arrow"/>

</RelativeLayout>